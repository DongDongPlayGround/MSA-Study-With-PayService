# Datasource
spring:
  datasource:
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://mysql:3306/my_fastcampus_pay?useSSL=false&allowPublicKeyRetrieval=true
#    username: user
#    password: pwd
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:banking
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  h2:
    console:
      enabled: true
      path: /h2-console